# BAR Controller Companion v0.8.5

v0.8.5 focuses on command insertion restoration, factory queue/quota clarity, and mobile type navigation.

The public release is built through `tools/release/Build-ControllerPublicRelease.ps1`, deployed through `tools/release/Deploy-ControllerPublicRelease.ps1`, and published with `tools/release/Publish-ControllerRelease.ps1 -Latest`.
