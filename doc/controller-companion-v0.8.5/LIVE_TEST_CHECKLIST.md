# v0.8.5 Live Test Checklist

- Verify `RB+X` inserts Smart X/native/staged commands at the front of the selected unit queue and does not open the build radial on RB release.
- Verify factory `RB+A` inserts the highlighted build at the front, queue counts refresh immediately, and progress wipe remains visible.
- Toggle factory Queue/Quota Mode with the factory LB+RB hold and confirm exact `Queue Mode` / `Quota Mode` toasts plus magenta `X/Y` quota counts.
- Verify `RB+D-pad Left/Right` cycles owned mobile unit types and `RB+D-pad Down` selects visible units of that type.
- Verify idle cycling ignores passive static radar/economy/defense structures and queued/producing factories.
- Verify Disassemble only arms for constructor/commander reclaim-capable selections and same-type reclaim starts with a compact radius.
- Verify Self Destruct hint appears only after Back/View is held and still requires both stick clicks held.
