# Historical release catalog

The trusted catalog contains only GitHub/committed identities with exact hashes: v0.6.0 (`9edd691f…`), v0.6.1 (`449f9281…`), and v0.7.0 (`18060e56…`). Eight packaged v0.8 milestones are published oldest-first with detached manifests derived from their untouched ZIPs and exact requested commit/hash mappings.

Manifestless compatibility entries preserve `LuaUI/Config`, `springsettings.cfg`, companion preferences/data, release cache, and recovery backups. Missing/unverifiable assets appear unavailable or are omitted; hashes are never invented. The publisher preserves the existing v0.7.0 release rather than duplicating it.
