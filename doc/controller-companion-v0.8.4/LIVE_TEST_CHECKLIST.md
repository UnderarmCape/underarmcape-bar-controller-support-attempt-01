# v0.8.4 Live Test Checklist

1. Confirm RB+A and RB+X insert active non-factory command confirmations, and confirm Y does not insert.
2. Press RT then RB, and RB then RT, inside the grace window. Build/Factory radial and Disassemble must not open, and RB must rearm only after neutral.
3. Smart X a damaged friendly unit, then Smart X an enemy reclaimable unit with a selected reclaimer, then check ordinary Smart X fallback and Hold-X move.
4. Cycle idle build/infrastructure with D-pad Left/Right, idle mobile with LB+D-pad Left/Right, then LB+D-pad Down for the remembered type.
5. In Disassemble, mark friendly targets, enemy targets, and a mixed collection. Friendly targets should use vanilla selection; enemies should use the controller overlay only.
6. Use LB+A tap on the mixed collection and verify one reclaim dispatch for the full collection. Clear the collection and verify LB+A falls back to the captured reticle target.
7. Use LB+Hold-A on a target type, release to resize the visible circle, confirm with A and X in separate attempts, cancel with B, and verify Disassemble remains active.
8. Press B through target/area/friendly-selection clear states, then double-B only when clear. Recheck LB+B Stop and L3+R3 Clear Queue.
9. Use the Tactical Self Destruct button and verify the direct protected custom flow; red/native-style presentation should remain.
